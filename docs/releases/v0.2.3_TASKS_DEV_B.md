# v0.2.3 TASKS — Dev Agent B (Tooling/QA/Docs)
**Last Updated:** 2026-02-14

## Workstreams
B-1 Preflight Gate (MANDATORY)
- Add `npm run preflight` (no-deps compatible) that checks:
  1) ProspectsWindow schema snapshot (v1)
  2) Determinism smoke (same seed/decisions ⇒ same ProspectsWindow ids+fields)
  3) Non-perturbation when no prospects accepted (golden seeds)

B-2 Stub Generator (MANDATORY)
- Add dev script: `npm run prospects:stub -- --seed=<seed> --turn=<t>`
- Emits sample `ProspectsWindow` JSON matching v1 (fixtures for UX/QA/Balance)
- Must be deterministic and use isolated RNG stream (prospects) without affecting sim outcomes.

B-3 UX lint (OPTIONAL but recommended)
- Add lightweight rule to prevent ad hoc prospect UI strings (if feasible without noise)

## Deliverable
- `devB_patch_v0.2.3.zip` (scripts/tests/docs only) + `DEV_NOTES.md`
