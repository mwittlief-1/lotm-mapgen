# v0.0.8 Regression Notes

**Version:** v0.0.8

**Date:** 2026-02-12

## Summary

- `npm run qa` (no-install path) completes successfully and produces `qa_artifacts/v0.0.8_no_deps_gate.json` with `ok: true`.

## Notable confirmations

- **No-deps QA gate is operational** (no `node_modules` required).
- **Bounded snapshots** are enforced (prevents nested snapshot/log growth regressions).
- **Policy registry drift prevention** is active via `docs/BUILD_INFO.json.policy_ids` matching the policy registry.

## Non-blocking observations

- `package.json` still reports `lords-of-the-manor-mvp@0.0.7` in npm’s script banner, while the build metadata and artifact report `v0.0.8`.
  - Recommend aligning package.json version string to avoid confusion in logs.

## Follow-up recommendations (QA infra, not balance)

- Consider adding a tiny CI job that runs **only**:
  - `rm -rf node_modules && npm run qa`
  - and asserts `qa_artifacts/*_no_deps_gate.json` exists with `ok: true`.
