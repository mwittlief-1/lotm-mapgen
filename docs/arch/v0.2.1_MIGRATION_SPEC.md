# v0.2.1 Migration Spec (v0.1.0 -> v0.2.1)

Target state adds:
- people: Record<PersonID, Person>
- houses: Record<HouseID, House>
- player_house_id
- kinship edges: parent_of, spouse_of

Guidance:
- Preserve existing person IDs where possible (pc_head, pc_spouse, pc_child_*) to avoid drift.
- Sort keys on iteration.
- Migration must be deterministic given input state.
